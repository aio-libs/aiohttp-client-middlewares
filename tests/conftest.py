"""Shared test fixtures.

Server/client fixtures (``aiohttp_server`` etc.) are provided by the
``pytest-aiohttp`` plugin; ``asyncio_mode = auto`` is configured in
``pyproject.toml`` so coroutine tests need no explicit marker.
"""
