Added :class:`~aiohttp_client_middlewares.RateLimitMiddleware`, a client-side
rate limiter with a pluggable algorithm (the ``RateLimiter`` base class, with
a token bucket included) and optional per-domain limiters
-- by :user:`rodrigobnogueira`.
