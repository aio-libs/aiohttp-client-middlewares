Stopped :class:`~aiohttp_client_middlewares.RateLimitMiddleware` failing every
request made under ``ClientTimeout(total=0)``. aiohttp arms its own deadline
only for a positive total, so zero means "no timeout"; the middleware now reads
it the same way instead of as a budget already spent
-- by :user:`rodrigobnogueira`.
