``aiohttp >= 3.15`` is now required. The rate limiter reads each request's
total timeout from ``ClientRequest.timeout``, which aiohttp exposes as a
read-only attribute from 3.15 onwards -- by :user:`rodrigobnogueira`.
